# SASL password for SMTP relay
# Format: [relayhost]:port  username:password
# After editing, run: docker compose run --rm smtp postmap /etc/postfix/sasl_passwd
#
[smtp.example.com]:587  noreply@yourdomain.com:YOUR_SMTP_PASSWORD